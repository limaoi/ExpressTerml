<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="RadioButton_Style">
        <item name="android:layout_width">0dp</item>
        <item name="android:padding">4dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:button">@null</item><!--去除RadioButton默认带的圆点-->
        <item name="android:gravity">center</item>
    </style>

    <style name="MyCustomTabLayout">
        <!-- 改变指示器下标的颜色-->
        <item name="tabIndicatorColor">@color/color_f08519</item>
        <!-- 改变选中字体的颜色-->
        <item name="tabSelectedTextColor">@color/color_f08519</item>
        <!-- 改变未选中字体的颜色-->
        <item name="tabTextColor">@color/color_666666</item>
        <!-- 改变背景的颜色-->
        <!--<item name="android:background">@color/colorWhite</item>-->
    </style>

</resources>
